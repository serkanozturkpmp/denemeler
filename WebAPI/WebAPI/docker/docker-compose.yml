version: '3'

services:
  db:
    image: postgres
    volumes:
      - db-data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
      - "8080:8080"

    environment:
      - POSTGRES_DB=psDb
      - POSTGRES_USER=serkan
      - POSTGRES_PASSWORD=1234

volumes:
  db-data: